<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Placed.RakeDetails">
    <ContentPage.Content>
        <StackLayout>
            <Label x:Name="LblTitle"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Label x:Name="lblDeadline" HorizontalOptions="Center" FontSize="Medium" />
            <ListView x:Name="WagonList" Margin="0,20,0,20" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding ID}">
                            
                        </TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button x:Name="btnPlace" HorizontalOptions="FillAndExpand" Clicked="btnPlace_Clicked" Text="Report Placement"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>